# Commit Message Rules

사용자가 **"커밋 생성"**이라고 입력하면 아래 규칙에 따라 현재 변경사항을 분석하고 커밋 메시지를 생성한다.

## 기본 동작

1. `git status`를 확인하여 현재 변경된 파일을 파악한다.
2. `git diff`를 확인하여 실제 코드 변경 내용을 파악한다.
3. 변경된 파일과 코드의 목적을 분석한다.
4. Conventional Commits 형식에 맞는 커밋 메시지를 생성한다.
5. 커밋 메시지만 제안하고 실제 커밋은 실행하지 않는다.

추가로 다음 규칙을 따른다.

* 현재 작업 트리의 변경 사항만 분석한다.
* 이미 커밋된 내용은 분석하지 않는다.
* 파일을 수정하지 않는다.
* `git add`, `git commit`, `git push`를 실행하지 않는다.
* 브랜치를 생성하거나 변경하지 않는다.
* `git merge`, `git rebase`를 실행하지 않는다.

## Commit Convention

Conventional Commits 형식을 사용한다.

형식:

`<type>: <summary>`

필요한 경우 body를 추가한다.

## Types

* `feat`: 새로운 기능
* `fix`: 버그 수정
* `refactor`: 기능 변경 없는 리팩터링
* `style`: 스타일/UI 수정
* `docs`: 문서 수정
* `test`: 테스트 추가/수정
* `chore`: 설정, 패키지, 빌드 등
* `perf`: 성능 개선

## Summary Rules

* 한국어로 작성한다.
* 한 줄로 간결하게 작성한다.
* 변경 내용을 구체적으로 표현한다.
* 변경된 코드가 **무엇을 했는지보다 왜 변경되었는지와 목적**이 드러나도록 작성한다.
* "수정", "변경"처럼 의미가 모호한 표현만 단독으로 사용하지 않는다.

## Multiple Changes

여러 변경사항이 하나의 작업 목적에 해당하면 하나의 커밋 메시지로 제안한다.

서로 다른 목적의 변경사항이 섞여 있다면 하나의 커밋으로 억지로 묶지 않는다.

분리하는 것이 더 적절하다면 다음 내용을 제안한다.

1. 각 커밋에 포함할 파일 또는 변경사항
2. 각 커밋의 커밋 메시지
3. 커밋을 분리하는 이유

## Output

가장 적절한 커밋 메시지 1개를 먼저 제안한다.

예시:

`feat: 모임 생성 플로팅 버튼 추가`

필요한 경우 커밋 메시지 아래에 간단한 설명을 추가한다.

여러 커밋으로 분리하는 것이 적절하다면 커밋 단위와 각각의 메시지를 제안한다.

## Safety

* 절대로 실제 커밋을 실행하지 않는다.
* 사용자의 명시적인 요청 없이 파일을 수정하지 않는다.
* `git add`, `git commit`, `git push`를 실행하지 않는다.
* 현재 브랜치를 변경하지 않는다.
