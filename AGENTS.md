# Commit & PR Rules

## 공통 원칙

Git 변경사항을 분석하여 커밋 메시지 또는 PR 제목/본문을 작성한다.

다음 안전 규칙을 항상 따른다.

* 실제 커밋이나 PR을 생성하지 않는다.
* 사용자의 명시적인 요청 없이 파일을 수정하지 않는다.
* `git add`, `git commit`, `git push`를 실행하지 않는다.
* 브랜치를 생성하거나 변경하지 않는다.
* `git merge`, `git rebase`를 실행하지 않는다.
* Git 상태와 변경사항을 확인하기 위한 읽기 전용 명령만 사용한다.

---

# Commit Message Rules

사용자가 **"커밋 생성"**이라고 입력하면 아래 규칙에 따라 현재 작업 트리의 변경사항을 분석하고 커밋 메시지를 생성한다.

## 기본 동작

1. `git status`를 확인하여 현재 변경된 파일을 파악한다.
2. `git diff`와 필요한 경우 `git diff --staged`를 확인하여 실제 코드 변경 내용을 파악한다.
3. 현재 브랜치명을 확인한다.
4. 브랜치명에서 이슈 번호를 확인한다.
5. 변경된 파일과 코드의 목적을 분석한다.
6. Conventional Commits 형식에 맞는 커밋 메시지를 생성한다.
7. 커밋 메시지만 제안하고 실제 커밋은 실행하지 않는다.

추가로 다음 규칙을 따른다.

* 현재 작업 트리의 변경사항만 분석한다.
* 이미 커밋된 내용은 커밋 메시지 생성에 포함하지 않는다.
* 파일을 수정하지 않는다.

## Commit Convention

Conventional Commits 형식을 사용한다.

형식:

`<type>: <summary> (#<issue-number>)`

예시:

`feat: 공통 모달 컴포넌트 추가 (#12)`

브랜치명에서 이슈 번호를 확인할 수 있다면 반드시 커밋 메시지 마지막에 추가한다.

예:

`feature/12-modal`

→

`feat: 다양한 콘텐츠를 지원하는 공통 모달 컴포넌트 추가 (#12)`

브랜치명에서 이슈 번호를 명확하게 확인할 수 없다면 임의로 생성하지 않고 사용자에게 이슈 번호를 확인한다.

## Types

* `feat`: 새로운 기능
* `fix`: 버그 수정
* `refactor`: 기능 변경 없는 리팩터링
* `style`: 스타일/UI 수정
* `docs`: 문서 수정
* `test`: 테스트 추가/수정
* `chore`: 설정, 패키지, 빌드 등
* `perf`: 성능 개선

## Summary Rules

* 한국어로 작성한다.
* 한 줄로 간결하게 작성한다.
* 변경 내용을 구체적으로 표현한다.
* 변경된 코드가 무엇을 했는지 나열하기보다 변경 목적이 드러나도록 작성한다.
* "수정", "변경"처럼 의미가 모호한 표현만 단독으로 사용하지 않는다.

## Multiple Changes

여러 변경사항이 하나의 작업 목적에 해당하면 하나의 커밋 메시지로 제안한다.

서로 다른 목적의 변경사항이 섞여 있다면 하나의 커밋으로 억지로 묶지 않는다.

분리하는 것이 더 적절하다면 다음 내용을 제안한다.

1. 각 커밋에 포함할 파일 또는 변경사항
2. 각 커밋의 커밋 메시지
3. 커밋을 분리하는 이유

## Commit Output

가장 적절한 커밋 메시지 1개를 먼저 제안한다.

예시:

`feat: 공통 모달 컴포넌트 추가 (#12)`

필요한 경우 커밋 메시지 아래에 간단한 설명을 추가한다.

여러 커밋으로 분리하는 것이 적절하다면 커밋 단위와 각각의 메시지를 제안한다.

이슈 번호를 생략한 경우 "이슈 번호를 확인할 수 없어 생략했습니다."​라고 사용자에게 알린다.

---

# Pull Request Rules

사용자가 **"PR 생성"**이라고 입력하면 현재 브랜치에서 base 브랜치로 제출할 전체 변경사항을 분석하여 PR 제목과 본문을 작성한다.

실제 PR은 생성하지 않는다.

## 기본 동작

1. 현재 브랜치를 확인한다.
2. base 브랜치를 확인한다.
3. 별도 지정이 없다면 `develop`을 base 브랜치로 사용한다.
4. `git log <base>..HEAD`를 확인하여 현재 PR에 포함될 커밋을 파악한다.
5. `git diff <base>...HEAD`를 확인하여 PR에 포함될 전체 코드 변경사항을 파악한다.
6. 현재 브랜치명에서 이슈 번호를 확인한다.
7. 전체 변경사항의 목적을 분석한다.
8. PR 제목과 PR 본문을 작성한다.

PR 분석에서는 현재 작업 트리의 `git diff`만 사용하지 않는다.

이미 커밋된 변경사항까지 포함하여 **base 브랜치와 현재 브랜치 사이의 전체 변경사항**을 기준으로 작성한다.

## PR Title

PR 제목은 변경사항의 핵심 목적이 드러나도록 작성한다.

기본적으로 Conventional Commits 형식을 사용한다.

형식:

`<type>: <summary> (#<issue-number>)`

예시:

`feat: 공통 모달 컴포넌트 구현 (#12)`

브랜치명에서 이슈 번호를 확인할 수 있다면 제목 마지막에 추가한다.

이슈 번호를 확인할 수 없다면 임의로 생성하지 않는다.

## PR Body

PR 본문은 다음 형식을 기본으로 사용한다.

### 작업 내용

* PR의 핵심 작업 목적을 요약한다.
* 단순한 파일 변경 목록보다 어떤 기능을 구현하거나 어떤 문제를 해결했는지 작성한다.

### 주요 변경 사항

* 실제 코드 변경사항을 기반으로 작성한다.
* 중요한 구현 내용만 간결하게 정리한다.
* 코드에서 확인할 수 없는 내용을 추측해서 작성하지 않는다.

### 테스트

* 코드에서 확인할 수 있는 테스트 또는 검증 내용을 작성한다.
* 테스트를 확인할 수 없다면 테스트를 수행했다고 임의로 작성하지 않는다.
* 필요한 경우 사용자가 확인할 항목을 제안한다.

### 관련 이슈

브랜치명에서 이슈 번호를 확인할 수 있다면 다음 형식으로 작성한다.

`Closes #<issue-number>`

예시:

`Closes #12`

이슈 번호를 확인할 수 없다면 임의의 번호를 작성하지 않는다.

## PR Output

다음 순서로 출력한다.

**PR 제목**

`feat: 공통 모달 컴포넌트 구현 (#12)`

**PR 내용**

```md
## 작업 내용

- 서비스 전반에서 재사용할 수 있는 공통 Modal 컴포넌트를 구현했습니다.

## 주요 변경 사항

- Header, Body, Footer 구조의 Modal 컴포넌트 구현
- 콘텐츠에 따른 Modal 크기 설정 지원
- Storybook을 통한 Modal UI 케이스 추가

## 테스트

- Storybook에서 Modal 열기/닫기 동작 확인
- Header/Footer 유무에 따른 UI 확인

## 관련 이슈

Closes #12
```

---

# Safety

* 절대로 실제 커밋을 실행하지 않는다.
* 절대로 실제 PR을 생성하지 않는다.
* 사용자의 명시적인 요청 없이 파일을 수정하지 않는다.
* `git add`, `git commit`, `git push`를 실행하지 않는다.
* 현재 브랜치를 변경하지 않는다.
* `git merge`, `git rebase`를 실행하지 않는다.
* 이슈 번호를 확인할 수 없는 경우 임의로 생성하지 않는다.
